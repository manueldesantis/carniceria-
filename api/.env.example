PORT=3080
MONGODB_URI=mongodb+srv://USUARIO:CLAVE@CLUSTER.mongodb.net/syncdbf?retryWrites=true&w=majority
MONGODB_DB=syncdbf
# Clave de acceso a la app: archivo api\claveclie.key (una sola linea)
# En hosting (Render/VPS) puede usar APP_PASSWORD como respaldo si no hay archivo
# APP_PASSWORD=JulioAbe2026
# URL fija publica HTTPS (Render / VPS / dominio). Se muestra en dist\URLs-*.txt
# PUBLIC_BASE_URL=https://clientebd-julioabe.onrender.com
# Opcional: limitar origen CORS (dominio de la URL publica)
# CORS_ORIGIN=https://clientebd-julioabe.onrender.com
